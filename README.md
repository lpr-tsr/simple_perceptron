# simple_perceptron
simple_perceptron by python with numpy lib.

* python3.5.1
* numpy
* matplotlib

* use virtualenv
`$ source ~/bin/activate`
* kill virtualenv
`$ deactivate`

`$python ~/src/single_perceptron.py`

he knows anything.